"""
# Creating an instance of the Cinemagoer class
ia = Cinemagoer()

# Searching movies that have rock in their name
myvar = easygui.enterbox("Insert Movie Name")
movies = ia.search_movie(myvar)
print(movies[0])
"""

for row in range(2,base.max_row+1):  
        sheet_title = "{}{}".format('C', row)
        sheet_year = "{}{}".format('E', row)
        if not base[sheet_title].value:
            break
        else:  
            searched = ia.search_movie(str(base[sheet_title].value))
            title = str(base[sheet_title].value)
            print(title)
            ourmovie = searched[0]
            for movie in searched:
                if str(base[sheet_year].value) == str(movie['year']):
                    ourmovie = movie
                    break
            row = metadata.get_highest_row() + 1
            new_row = [ourmovie['title'], 'data2', 'data3', 'data4']
            for col, entry in enumerate(new_row, start=1):
                ws.cell(row=row, column=col, value=entry)